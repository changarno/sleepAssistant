<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mio.sleepassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sleepassistantlogo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsLoopOnActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PlayerActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PlayerOnActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PlaylistActivity" />
        <activity android:name=".FaveActivity" />
        <activity android:name=".Player2Activity" />
        <activity android:name=".Player2OnActivity"></activity>
    </application>

</manifest>